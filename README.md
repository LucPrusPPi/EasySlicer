# EasySlicer

Блочный конструктор: разбей строку → кликни поля → склей → копируй.

```bat
pip install -r requirements.txt
python main.py
```

Шаблоны сохраняешь сам в `%USERPROFILE%\.easy_slicer\recipes.json`.
